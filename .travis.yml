---
language: php
sudo: false
cache:
  directories:
    - $HOME/.composer/cache/files
env:
  - SYMFONY_VERSION="4.0.*" DB=sqlite
before-install:
  - cp .env.dist .env
install:
  - composer install
script:
  - bin/console app:data:import
